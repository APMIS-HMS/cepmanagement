export interface NewEntry {
    serviceName: String;
    name: String;
};

export interface CustomEntry {
    serviceName: String;
    name: String;
}

export class CustomServiceName {
    name: String;
}

